function [w] = weightCalculator(weightMatrix, pixelValue)
%WEIGHT Summary of this function goes here
%   Detailed explanation goes here
w = 0;
[a, b] = size(weightMatrix);
linearSize = a * b;
center = ceil(linearSize / 2);

for i = 1:linearSize
    w = w + f(pixelValue, 
end

end

